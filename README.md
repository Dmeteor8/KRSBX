# 💰 业务报销记账系统

> 专为外勤销售人员设计的轻量级 Web 报销记账工具，支持移动端操作，数据云端实时同步。

## ✨ 项目简介

本项目是一个单页应用（SPA），无需复杂的后端环境部署。它集成了里程自动计算、费用明细录入、发票上传以及 Excel 报表导出功能。数据存储于 LeanCloud，确保多端数据一致性。

## 🌟 核心功能

- **🚗 智能里程计算**：只需输入“出发里程”和“回程里程”，系统自动计算当日总里程及油补。
- **☁️ 云端同步**：基于 LeanCloud 云端存储，换设备不丢失数据，支持多人注册使用。
- **📝 费用管理**：支持过路费、停车费、高铁费、住宿费等 8 类费用明细录入。
- **📸 发票上传**：支持上传发票/小票图片，记录随时可查。
- **📊 数据统计**：支持按月、按年查看报销汇总，一键导出 Excel 报表。
- **📱 移动端适配**：采用 Tailwind CSS 设计，界面简洁美观，完美适配手机浏览器。

## 🛠️ 技术栈

- **前端框架**：原生 HTML5 / JavaScript (ES6+)
- **UI 样式**：Tailwind CSS (CDN)
- **后端/存储**：LeanCloud (JavaScript SDK)
- **数据处理**：SheetJS (XLSX)
- **图标**：SVG Icons

## 🚀 快速开始

### 1. 克隆代码
将代码下载到本地，或直接在 Gitee 点击“克隆/下载”。

### 2. 配置 LeanCloud (重要)
为了使用云端同步功能，你需要拥有自己的 LeanCloud 账号并创建应用。

1.  注册/登录 [LeanCloud 控制台](https://console.leancloud.cn/)。
2.  创建一个应用，进入 **设置 -> 应用凭证**。
3.  找到 `AppID`、`AppKey` 和 `服务器地址`。
4.  打开项目中的 `index.html` 文件，找到 `AV.init` 部分，替换为你自己的凭证：

```javascript
AV.init({
    appId: "你的AppID",           
    appKey: "你的AppKey",         
    serverURL: "你的服务器地址(通常以lc-cn-n1-shared.com结尾)"    
});
